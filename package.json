{
  "name": "studio-susino-web",
  "version": "1.0.0",
  "private": true,
  "description": "Sito statico dello Studio Ecografico Dr. Salvatore Susino",
  "scripts": {
    "generate": "node scripts/genera-pagine-esami.js && node scripts/normalize-html.mjs && node scripts/generate-sitemap.js",
    "audit": "node scripts/audit-site.mjs",
    "test": "node --test tests/*.test.js && npm run audit",
    "build": "npm run generate && npm test"
  },
  "engines": {
    "node": ">=20"
  }
}
